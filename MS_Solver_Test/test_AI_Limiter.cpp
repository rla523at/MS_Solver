#pragma once
#include "gtest/gtest.h"
#include "../MS_Solver/INC/Reconstruction_Method.h"

GTEST_TEST(AI_Limiter, constructor) {
	//const auto grid = Grid_Builder<2>::build<Gmsh>("Quad4");
	//AI_limiter<Vertex_Least_Square<4, 2>> limiter(grid);

	//for (const auto& text : limiter.ai_limiter_text_set_)
	//	std::cout << text;
}